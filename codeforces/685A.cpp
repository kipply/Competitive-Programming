#include <bits/stdc++.h>

using namespace std;

int n, s[3003], c[3003]; 

int main(){
  scanf("%llu %llu", &x, &y);
  if (log(x) > log(y)) cout << "<"; 
  else if (log(y) > log(x)) cout << ">"; 
  else cout << "=";
}

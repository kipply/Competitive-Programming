#include <bits/stdc++.h>
using namespace std;


int a, b, c; 

int main() { 
  scanf("%d %d %d", &a, &b, &c);
  if (a % 2 && b % 2) {
    cout << "Yes"; 
  } else {
    cout << "No";
  }
  return 0;
} 